# LocalStack Configuration
LOCALSTACK_VERSION=latest
LOCALSTACK_VOLUME_DIR=./volume
DEBUG=1

# AWS Configuration
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Application Configuration
NODE_ENV=production
PORT=3031

# API Configuration
API_BASE_URL=https://localcloudkit.local/api
API_INTERNAL_URL=http://api:3031

# GUI Configuration
# Frontend uses relative /api paths (no env var needed)
# NEXT_PUBLIC_API_URL=/api

# CORS Configuration
CORS_ORIGIN=https://localcloudkit.local
SOCKET_IO_ORIGIN=https://localcloudkit.local

# Docker Configuration
COMPOSE_PROJECT_NAME=localstack-template 