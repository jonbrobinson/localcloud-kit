# S3 (object storage)

LocalCloud Kit emulates **Amazon S3** through the AWS Emulator (MiniStack). Buckets and objects work like AWS S3; use `http://localhost:4566` from the host or `http://aws-emulator:4566` from other containers.

For SDK and CLI examples, see **[docs/CONNECT.md](CONNECT.md)**. The in-app doc page (same content plus connection table) is **`/s3`** in the GUI.

## Manage S3 (`/manage/s3`)

Open **Resources → S3 Buckets → Manage**, or use **Open Manager** on the **`/s3`** doc page.

- Pick a bucket, then browse **folder prefixes** (nested keys) the same way as in the dashboard bucket viewer.
- **Breadcrumbs**: The **bucket name** is a control that returns you to that bucket’s **root** (empty prefix). Each **folder segment** in the path is clickable and jumps to that prefix; the **last** segment is the current folder (not a separate link). **Back** walks up the folder stack and stays aligned with the path shown in the breadcrumbs.

## Dashboard vs Manage S3

| Where | Role |
| ----- | ---- |
| **Dashboard** | Create/destroy buckets, open a bucket in a modal, quick uploads and object actions |
| **Manage S3** | Dedicated full-page browser with the same prefix-style folder navigation and breadcrumb behavior |

## Limits and behavior

- Multipart uploads are supported up to **100MB** per object from the GUI (see README feature list).
- **Nested “folders”** are key prefixes; listing uses delimiter-style semantics so you see common prefixes as folders.

## API (Express)

Base path: `/api` (via Traefik: `https://app-local.localcloudkit.com:3030/api`).

| Method | Endpoint | Description |
| ------ | -------- | ----------- |
| `GET` | `/api/s3/buckets` | List buckets; optional `?projectName=` |
| `GET` | `/api/s3/bucket/:bucketName/contents` | List bucket contents; optional `?projectName=` |
| `GET` | `/api/s3/bucket/:bucketName/object/*path` | Download object body + metadata (`?projectName=` required) |
| `POST` | `/api/s3/bucket/:bucketName/upload-multipart` | Multipart upload (`projectName`, `objectKey`, file) |

## Shell scripts

See **[docs/SETUP_SCRIPTS.md](SETUP_SCRIPTS.md)** and `scripts/shell/` for bucket and object automation (e.g. `create_single_resource.sh`, `upload_s3_object.sh`, `download_s3_object.sh`).

## Troubleshooting

- Confirm the emulator is up: `curl http://localhost:4566/_localstack/health`
- CLI: `aws --endpoint-url=http://localhost:4566 s3 ls`

More context: **[docs/AWS_EMULATOR.md](AWS_EMULATOR.md)**, **[docs/TROUBLESHOOTING.md](TROUBLESHOOTING.md)**.
